# Copy this file to secrets.toml and fill in your values.
# secrets.toml is gitignored — never commit it.

APP_PASSWORD        = "choose_a_personal_password"

GEMINI_API_KEY      = "AIza..."      # aistudio.google.com/app/apikey
YOUTUBE_API_KEY     = "AIza..."      # console.cloud.google.com → YouTube Data API v3
GROQ_API_KEY        = "gsk_..."      # console.groq.com  (optional fallback)

# Fine-grained GitHub PAT scoped to this repo with Contents: Read+Write
GITHUB_PAT          = "ghp_..."
GITHUB_REPO_OWNER   = "celusddb07"
GITHUB_REPO_NAME    = "daily-learner"
GITHUB_BRANCH       = "main"
